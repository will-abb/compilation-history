#!/usr/bin/env bash
#MISE description="Run all MELPA readiness checks (byte-compile, package-lint, checkdoc, tests)"
#MISE depends=["byte-compile", "package-lint", "checkdoc", "test"]
set -euo pipefail

echo ""
echo "All MELPA readiness checks passed!"
